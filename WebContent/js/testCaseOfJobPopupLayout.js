var parentTreeData;
var selectedNodeDetails;
var nodeDropped = false;

var TestCaseOfJobsPagination = function() {
	
	   var initialise = function(jsonObj){
		   //showTreePagination(jsonObj);		   
	   };
			return {				
	        //main function to initiate the module
	        init: function(jsonObj) {        	
	        	initialise(jsonObj);
	        }		
		};	
}();
